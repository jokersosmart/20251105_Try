# PowerShell 中文亂碼問題修復指南

## 🐛 問題說明

在 PowerShell 中執行 Git 命令時，中文字元顯示為亂碼（例如：`�۰� Commit �O�W�w���J`）。

**原因**: 
1. PowerShell 預設編碼與終端顯示編碼不匹配
2. Git 預設會轉義非 ASCII 字元

**影響**: 
- ✅ Git 操作正常運作（推送成功）
- ❌ 中文輸出顯示為亂碼
- ❌ 影響閱讀和除錯體驗

**✅ 已執行的修復**:
- `git config --global core.quotepath false` ✅ 已設定
- `git config --global i18n.commitencoding utf-8` ✅ 已設定
- `git config --global i18n.logoutputencoding utf-8` ✅ 已設定
- PowerShell Profile UTF-8 設定 ✅ 已添加

---

## ✅ 解決方案（完整步驟）

### 方法 1: 永久修復（推薦）⭐

#### 步驟 1: 重新建立 PowerShell Profile（使用 UTF-8 編碼）

**在 PowerShell 中執行**：

```powershell
# 備份現有 Profile
Copy-Item $PROFILE "$PROFILE.backup" -Force

# 讀取現有內容（使用 UTF-8）
$profileContent = Get-Content $PROFILE -Raw -Encoding UTF8

# 添加 UTF-8 編碼設定（在最前面）
$utf8Header = @"
# ========================================
# PowerShell 編碼設定（解決中文亂碼）
# ========================================
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
`$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001 | Out-Null

"@

# 檢查是否已存在編碼設定
if ($profileContent -notmatch "OutputEncoding.*UTF8") {
    $newContent = $utf8Header + $profileContent
    Set-Content -Path $PROFILE -Value $newContent -Encoding UTF8
    Write-Host "✅ PowerShell Profile 已更新為 UTF-8 編碼" -ForegroundColor Green
} else {
    Write-Host "ℹ️ UTF-8 編碼設定已存在" -ForegroundColor Yellow
}
```

#### 步驟 2: 重新啟動 PowerShell

```powershell
# 關閉當前終端視窗，然後開啟新的 PowerShell 視窗
```

#### 步驟 3: 驗證修復

```powershell
# 測試中文顯示
Write-Host "測試中文：這是繁體中文" -ForegroundColor Green

# 檢查編碼
[Console]::OutputEncoding
# 應顯示：UTF8

# 測試 Git 命令
git status
# 中文應正常顯示
```

---

### 方法 2: 臨時修復（每次開啟終端需執行）

在每次開啟 PowerShell 時執行：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001
```

或建立一個快捷命令：

```powershell
# 在 Profile 中添加函數
function Fix-Encoding {
    [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
    $OutputEncoding = [System.Text.Encoding]::UTF8
    chcp 65001 | Out-Null
    Write-Host "✅ 編碼已設定為 UTF-8" -ForegroundColor Green
}

Set-Alias -Name utf8 -Value Fix-Encoding
```

然後使用：
```powershell
utf8  # 快速修復編碼
```

---

### 方法 3: 修復 PowerShell Profile 檔案本身的編碼

如果 Profile 檔案儲存時使用了錯誤的編碼，需要重新儲存：

```powershell
# 讀取並重新儲存為 UTF-8（含 BOM）
$content = Get-Content $PROFILE -Raw -Encoding UTF8
[System.IO.File]::WriteAllText($PROFILE, $content, [System.Text.UTF8Encoding]::new($true))

Write-Host "✅ Profile 檔案已轉換為 UTF-8-BOM 編碼" -ForegroundColor Green
```

---

## 🔍 診斷當前編碼設定

執行以下命令檢查當前編碼狀態：

```powershell
# 檢查 Console 編碼
Write-Host "Console Output Encoding: $([Console]::OutputEncoding.EncodingName)"

# 檢查 PowerShell 變數編碼
Write-Host "PowerShell Output Encoding: $($OutputEncoding.EncodingName)"

# 檢查 Code Page
Write-Host "Code Page: $(chcp)"

# 檢查 Profile 位置
Write-Host "Profile Path: $PROFILE"

# 測試中文輸出
Write-Host "`n測試輸出：" -ForegroundColor Cyan
Write-Host "繁體中文測試：你好世界" -ForegroundColor Green
Write-Host "Git 推送成功！" -ForegroundColor Green
```

**期望的正確輸出**：
```
Console Output Encoding: Unicode (UTF-8)
PowerShell Output Encoding: Unicode (UTF-8)
Code Page: Active code page: 65001
```

---

## ⚙️ VS Code 終端設定

如果您在 VS Code 內建終端中使用 PowerShell，也需要確保 VS Code 設定正確：

### 更新 VS Code settings.json

```json
{
  "terminal.integrated.defaultProfile.windows": "PowerShell",
  "terminal.integrated.profiles.windows": {
    "PowerShell": {
      "source": "PowerShell",
      "args": ["-NoExit", "-Command", "chcp 65001 | Out-Null"]
    }
  },
  "files.encoding": "utf8",
  "files.autoGuessEncoding": false
}
```

---

## 🚀 一鍵修復腳本

我為您創建了一個一鍵修復腳本：

**檔案**: `fix-encoding.ps1`

```powershell
# 一鍵修復 PowerShell 中文亂碼問題

Write-Host "`n🔧 開始修復 PowerShell 編碼問題..." -ForegroundColor Cyan

# 1. 設定當前會話編碼
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001 | Out-Null

Write-Host "✅ 當前會話編碼已設定為 UTF-8" -ForegroundColor Green

# 2. 更新 PowerShell Profile
$utf8Header = @"

# ========================================
# PowerShell 編碼設定（解決中文亂碼）
# ========================================
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
`$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001 | Out-Null

"@

if (Test-Path $PROFILE) {
    $profileContent = Get-Content $PROFILE -Raw -Encoding UTF8
    
    if ($profileContent -notmatch "# PowerShell 編碼設定") {
        # 在檔案開頭插入編碼設定
        $newContent = $utf8Header + $profileContent
        Set-Content -Path $PROFILE -Value $newContent -Encoding UTF8
        Write-Host "✅ PowerShell Profile 已更新（添加 UTF-8 設定）" -ForegroundColor Green
    } else {
        Write-Host "ℹ️  PowerShell Profile 已包含 UTF-8 設定" -ForegroundColor Yellow
    }
} else {
    Set-Content -Path $PROFILE -Value $utf8Header -Encoding UTF8
    Write-Host "✅ PowerShell Profile 已建立（含 UTF-8 設定）" -ForegroundColor Green
}

# 3. 測試中文顯示
Write-Host "`n📝 測試中文顯示：" -ForegroundColor Cyan
Write-Host "你好，世界！" -ForegroundColor White
Write-Host "繁體中文測試成功！" -ForegroundColor Green
Write-Host "Git 推送完成！" -ForegroundColor Green

Write-Host "`n✅ 修復完成！請重新啟動 PowerShell 終端以套用變更。" -ForegroundColor Green
Write-Host "💡 提示：關閉此終端視窗，開啟新的 PowerShell 視窗即可。" -ForegroundColor Yellow
```

---

## 📝 使用方式

### 立即修復

```powershell
# 執行一鍵修復腳本
.\fix-encoding.ps1

# 或手動執行
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001
```

### 重新啟動終端

**最重要的步驟**：關閉當前 PowerShell 終端視窗，開啟新的視窗。

修復後的效果：
- ✅ 中文正常顯示
- ✅ Git 輸出清晰可讀
- ✅ 錯誤訊息正確顯示

---

## 🔧 進階解決方案

### 如果上述方法仍無效

#### 選項 1: 使用 Windows Terminal

Windows Terminal 對 UTF-8 支援更好：

1. 安裝 Windows Terminal（從 Microsoft Store）
2. 設定預設 Shell 為 PowerShell
3. 設定會自動套用 UTF-8

#### 選項 2: 修改 VS Code 使用 CMD 或 Git Bash

在 `.vscode/settings.json` 中：

```json
{
  "terminal.integrated.defaultProfile.windows": "Git Bash"
}
```

Git Bash 對中文支援更好，不會有亂碼問題。

---

## ✅ 驗證修復成功

修復後，執行以下命令驗證：

```powershell
# 1. 檢查編碼
[Console]::OutputEncoding
# 應顯示：BodyName : utf-8

# 2. 測試中文
Write-Host "中文測試：成功" -ForegroundColor Green
# 應正常顯示中文

# 3. 測試 Git
git status
# 中文訊息應正常顯示

# 4. 測試自動提交
.\commit.ps1
# 如有中文訊息應正常顯示
```

---

## 📚 相關資訊

**編碼代碼說明**：
- `65001` = UTF-8 Code Page
- `950` = Big5（繁體中文傳統編碼，可能導致問題）
- `UTF8` = Unicode UTF-8（推薦）

**PowerShell Profile 位置**：
```powershell
$PROFILE
# 通常在：C:\Users\[username]\Documents\PowerShell\Microsoft.PowerShell_profile.ps1
```

---

## 💡 重要提醒

1. **修復後必須重新啟動終端**才能完全生效
2. 如果在 VS Code 中，請重新載入視窗（Ctrl+Shift+P → "Reload Window"）
3. 備份檔案已建立在 `$PROFILE.backup`，如有問題可恢復

---

**建立時間**: 2025-11-19  
**適用環境**: Windows 10/11 + PowerShell 7+

